diff --git a/package.json b/package.json
index 3e40c53..e613203 100644
--- a/package.json
+++ b/package.json
@@ -34,13 +34,16 @@
   },
   "files": [
     "lib/*",
-    "dist/*"
+    "dist/*",
+    "package.json",
+    "rollup.config.js"
   ],
   "dependencies": {
     "ky": "^1.0.1",
     "undici": "^6.6.2"
   },
   "devDependencies": {
+    "@rollup/plugin-node-resolve": "^15.2.3",
     "c8": "^7.13.0",
     "chai": "^4.3.7",
     "cors": "^2.8.5",
diff --git a/rollup.config.js b/rollup.config.js
index f08fbaa..07e7e62 100644
--- a/rollup.config.js
+++ b/rollup.config.js
@@ -1,3 +1,4 @@
+import resolve from '@rollup/plugin-node-resolve';
 import pkg from './package.json' assert {type: 'json'};
 
 function preserveDynamicImportPlugin() {
@@ -18,7 +19,15 @@ export default [
         format: 'cjs'
       }
     ],
-    plugins: [preserveDynamicImportPlugin()],
+    plugins: [
+      resolve({
+        browser: true,
+      }),
+      resolve({
+        'react-native': true,
+      }),
+      preserveDynamicImportPlugin()
+    ],
     external: Object.keys(pkg.dependencies).concat(['crypto', 'util'])
   }
 ];
